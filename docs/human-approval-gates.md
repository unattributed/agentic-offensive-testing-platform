# Human approval gates

Active fuzzing, state-changing workflows, cross-account confirmation, lockout risk, a possible valid vulnerability, and report submission require a queued human decision.

The campaign records the pending reason and pauses. Approval applies only to the named objective and does not expand scope. Denial or expiry leaves the objective stopped. Report submission remains outside automated execution.
