# Sprint 0 Slice 0.1

Initial commit `094e756` created the package metadata, README, ignores, examples, configuration
tree, source package, tests, and development plan.

Current focused validation:

```text
python3 -m aotp.cli --help
usage: aotp [-h] {validate-config,list-cases,list-modules,...}
```

The package also builds successfully as both a source archive and wheel in current closeout
validation. Examples remain inert and use aliases or reserved placeholder domains.
