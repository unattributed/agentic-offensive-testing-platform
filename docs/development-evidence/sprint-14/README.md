# Sprint 14 Development Evidence

Sprint 14 implements the local Ollama plus LangChain Deep Agent campaign runtime, bounded private
workspace, supervisor and subagents, strict proposals, deterministic proposal gate, native
metadata tools, classified evidence summaries, three-iteration loop, authorized operator demo, and
sprint documentation.

Closeout evidence records only sanitized commands, hashes, versions, alias-only results, and
acceptance facts. Live target names, private authorization documents, raw evidence, and campaign
workspaces remain ignored under `.aotp/`.
