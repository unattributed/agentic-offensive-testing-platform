# Candidate evaluation example

This directory contains placeholders only. A real evaluator profile, provisioned account, target, authorization reference, or evidence must be stored outside version control in a private scope file.

Run planning with `config/scope.example.yaml`. Do not use the placeholder as live authorization.
